name: solo-unicorn-test

services:
  workspace:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: sleep infinity
    volumes:
      - ..:/workspaces/solo-unicorn-test:cached
    environment:
      DATABASE_URL: "postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@${POSTGRES_HOST:-db}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-solo_unicorn_test}"
      POSTGRES_HOST: ${POSTGRES_HOST:-db}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      POSTGRES_DB: ${POSTGRES_DB:-solo_unicorn_test}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-dev-only-secret-key}
      DJANGO_DEBUG: "${DJANGO_DEBUG:-1}"
      DJANGO_ALLOWED_HOSTS: "${DJANGO_ALLOWED_HOSTS:-*}"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - solo-unicorn-test-net

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: bash -lc "cd /workspaces/solo-unicorn-test/apps/api && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ..:/workspaces/solo-unicorn-test:cached
    environment:
      DATABASE_URL: "postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@${POSTGRES_HOST:-db}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-solo_unicorn_test}"
      POSTGRES_HOST: ${POSTGRES_HOST:-db}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      POSTGRES_DB: ${POSTGRES_DB:-solo_unicorn_test}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-dev-only-secret-key}
      DJANGO_DEBUG: "${DJANGO_DEBUG:-1}"
      DJANGO_ALLOWED_HOSTS: "${DJANGO_ALLOWED_HOSTS:-*}"
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - solo-unicorn-test-net

  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: bash -lc "cd /workspaces/solo-unicorn-test && npm --prefix apps/web install && npm --prefix apps/web run dev -- --hostname 0.0.0.0 --port 3000"
    volumes:
      - ..:/workspaces/solo-unicorn-test:cached
    ports:
      - "3000:3000"
    environment:
      API_BASE_URL: ${API_BASE_URL:-http://api:8000}
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
    depends_on:
      api:
        condition: service_started
    networks:
      - solo-unicorn-test-net

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-solo_unicorn_test}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - solo-unicorn-test-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-solo_unicorn_test}"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - solo-unicorn-test-net

volumes:
  solo-unicorn-test-postgres-data:

networks:
  solo-unicorn-test-net:
