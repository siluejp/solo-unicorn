openapi: 3.1.0
info:
  title: Solo Unicorn API
  version: 0.1.0
paths:
  /api/health/:
    get:
      operationId: getHealth
      summary: Health check endpoint
      responses:
        "200":
          description: API is healthy
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                properties:
                  status:
                    type: string
                    const: ok
