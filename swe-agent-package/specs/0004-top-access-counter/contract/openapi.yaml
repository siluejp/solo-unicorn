openapi: 3.1.0
info:
  title: Solo Unicorn API
  version: 0.1.0
paths:
  /api/top-access/:
    get:
      operationId: getTopAccess
      summary: Get current top page access count
      responses:
        "200":
          description: Current count
          content:
            application/json:
              schema:
                type: object
                required: [page, count]
                properties:
                  page:
                    type: string
                    const: top
                  count:
                    type: integer
                    minimum: 0
    post:
      operationId: incrementTopAccess
      summary: Increment and get top page access count
      responses:
        "200":
          description: Incremented count
          content:
            application/json:
              schema:
                type: object
                required: [page, count]
                properties:
                  page:
                    type: string
                    const: top
                  count:
                    type: integer
                    minimum: 1
